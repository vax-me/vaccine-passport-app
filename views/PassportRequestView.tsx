import React from "react";
import { Button, SafeAreaView, ScrollView, TextInput } from "react-native";

export const PassportRequestView = () => {
  return <SafeAreaView>
    <ScrollView>
      <TextInput>

      </TextInput>
      <Button onPress={() => {}} title={"Request"}>
        Request Passport
      </Button>
    </ScrollView>
  </SafeAreaView>
}
