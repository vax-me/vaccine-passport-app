import React from 'react';
import { Body, Header, Left, Subtitle, Title } from "native-base";

const CustomHeader = () => {
  return (<Header>
    <Body>
      <Title>Vax-Me</Title>
      <Subtitle>Your Personal Vaccine Passport</Subtitle>
    </Body>
  </Header>)
};

export default CustomHeader;
